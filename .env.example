# Local development can continue using SQLite with no variables.
# Set both values in Vercel (Production, Preview, and Development).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
SECRET_KEY=replace-with-a-long-random-secret
